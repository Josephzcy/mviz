SET(CMAKE_CXX_STANDARD 17)
SET(CORE_HOME_PATH ${MVIZ_HOME}/core)

FILE(GLOB MODULE_DIRECTORIES ${CORE_HOME_PATH}/*)


foreach(DIR ${MODULE_DIRECTORIES})
  IF(IS_DIRECTORY ${DIR})
  MESSAGE(STATUS "DIR: ${DIR}")
  ADD_SUBDIRECTORY(${DIR})
  ENDIF()
endforeach()


